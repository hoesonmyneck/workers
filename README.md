# Справочник сотрудников АО ЦРТР

Современное веб-приложение для управления справочником сотрудников АО "Центр развития трудовых ресурсов".

## Возможности

### Для обычных пользователей (без авторизации)
- 📋 Просмотр списка всех сотрудников
- 🔍 Поиск по ФИО, email, телефону, должности
- 🎯 Фильтрация по департаментам, кабинетам, должностям
- 📱 Адаптивный дизайн для мобильных устройств

### Для администраторов
- ✏️ Добавление новых сотрудников
- 🔄 Редактирование данных сотрудников
- 🗑️ Удаление записей
- 📊 Статистика и аналитика
- 📝 Просмотр логов всех действий
- 🔐 Безопасная аутентификация

## Технологии

- **Frontend**: Next.js 14, React, TypeScript, Tailwind CSS
- **Backend**: Next.js API Routes
- **База данных**: PostgreSQL (Supabase)
- **Аутентификация**: JWT токены
- **UI**: Custom components (Shadcn/ui style)

## Установка и запуск

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка базы данных

База данных уже настроена и данные мигрированы. Если нужно повторно мигрировать данные:

```bash
cd ..
python migrate_to_supabase.py
```

### 3. Настройка переменных окружения

Файл `.env.local` уже создан с необходимыми настройками:
- `DATABASE_URL` - подключение к Supabase PostgreSQL
- `JWT_SECRET` - секретный ключ для JWT токенов
- `NEXT_PUBLIC_APP_URL` - URL приложения

### 4. Запуск приложения

```bash
# Режим разработки
npm run dev

# Production сборка
npm run build
npm start
```

Приложение будет доступно по адресу: http://localhost:3000

## Учетные данные

### Администратор по умолчанию
- **Email**: admin@crer.kz
- **Пароль**: admin123

⚠️ **ВАЖНО**: Обязательно смените пароль администратора в продакшене!

## Структура проекта

```
workers-directory/
├── app/                      # Next.js App Router
│   ├── api/                 # API маршруты
│   │   ├── auth/           # Аутентификация
│   │   ├── employees/      # CRUD сотрудников
│   │   ├── filters/        # Фильтры
│   │   └── logs/           # Логи
│   ├── admin/              # Административная панель
│   │   ├── dashboard/     # Главная панель
│   │   ├── employees/     # Управление сотрудниками
│   │   ├── logs/          # Просмотр логов
│   │   └── login/         # Страница входа
│   ├── page.tsx           # Публичная главная страница
│   └── layout.tsx         # Корневой layout
├── components/             # React компоненты
│   └── ui/                # UI компоненты
├── lib/                   # Утилиты и хелперы
│   ├── db.ts             # Работа с БД
│   ├── auth.ts           # Аутентификация
│   ├── logger.ts         # Логирование
│   └── utils.ts          # Общие утилиты
└── public/               # Статические файлы
```

## Основные функции

### Публичный справочник (/)
- Просмотр всех сотрудников
- Поиск и фильтрация
- Постраничная навигация
- Контактная информация с кликабельными ссылками

### Административная панель (/admin)

#### Панель управления (/admin/dashboard)
- Статистика по сотрудникам
- Количество департаментов и должностей
- Быстрые ссылки на основные функции

#### Управление сотрудниками (/admin/employees)
- Просмотр всех сотрудников
- Добавление новых записей
- Редактирование существующих
- Удаление записей
- Поиск и фильтрация

#### Логи действий (/admin/logs)
- История всех действий администраторов
- Фильтрация по типу действия
- Детальная информация о каждом изменении
- Кто, когда и что изменил

## Безопасность

- ✅ JWT токены для аутентификации
- ✅ HTTP-only cookies
- ✅ Хеширование паролей (bcrypt)
- ✅ Защита API маршрутов
- ✅ SQL injection защита (параметризованные запросы)
- ✅ Логирование всех действий администраторов

## Логирование

Все действия администраторов логируются:
- **Вход/Выход** из системы
- **Создание** новых записей
- **Обновление** существующих записей
- **Удаление** записей
- **Изменение** настроек фильтров и столбцов

Для каждого действия сохраняется:
- Email администратора
- Тип действия
- Старые и новые значения
- Дата и время
- IP адрес (если доступен)

## API Endpoints

### Публичные
- `GET /api/employees` - Получить список сотрудников
- `GET /api/filters` - Получить доступные фильтры

### Защищенные (требуют авторизации)
- `POST /api/employees` - Создать сотрудника
- `PUT /api/employees/[id]` - Обновить сотрудника
- `DELETE /api/employees/[id]` - Удалить сотрудника
- `GET /api/logs` - Получить логи

### Аутентификация
- `POST /api/auth/login` - Вход
- `POST /api/auth/logout` - Выход
- `GET /api/auth/me` - Текущий пользователь

## Разработка

### Добавление нового администратора

Выполните SQL запрос в Supabase:

```sql
INSERT INTO admins (email, password_hash, full_name)
VALUES (
  'new-admin@crer.kz',
  -- Используйте bcrypt хеш пароля
  '$2a$10$hash...',
  'Имя Администратора'
);
```

### Изменение пароля

```sql
UPDATE admins 
SET password_hash = '$2a$10$new_hash...'
WHERE email = 'admin@crer.kz';
```

## Deployment

### Vercel (рекомендуется)

1. Подключите репозиторий к Vercel
2. Добавьте переменные окружения:
   - `DATABASE_URL`
   - `JWT_SECRET`
   - `NEXT_PUBLIC_APP_URL`
3. Deploy!

### Другие платформы

Приложение совместимо с любой платформой, поддерживающей Next.js:
- Netlify
- Railway
- Render
- DigitalOcean App Platform

## Поддержка

При возникновении проблем:
1. Проверьте подключение к базе данных
2. Убедитесь, что все переменные окружения установлены
3. Проверьте логи приложения
4. Проверьте таблицу `admin_logs` для отслеживания действий

## Лицензия

© 2025 АО "Центр развития трудовых ресурсов"

---

**Версия**: 1.0.0  
**Дата создания**: 29 января 2025
